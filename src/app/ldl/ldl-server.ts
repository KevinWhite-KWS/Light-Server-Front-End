export class LdlServer {
    constructor(server: string, name: string, ip:string) {
      this.server = server;
      this.name = name;
      this.ip = ip;
    }
  
  server: string;
  name: string;
  ip:string;
}
